[project]
name = "followback"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "instascrape>=0.1",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
    "mypy",
    "uv",
]
